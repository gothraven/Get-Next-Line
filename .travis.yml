language: c

compiler:
  - gcc

sudo: false

before_install:
  - pip install --user cpp-coveralls

script:
  - make && make test

after_success:
  - coveralls --exclude libft --exclude tests --gcov-options '\-lp'
